# Names every resource in the release. Keep it "snipit" — docker-build-push.yml's GitOps step
# patches apps[].image.tag in this file by app name, so the two app entries must stay named
# "backend" and "frontend".
fullnameOverride: "snipit"

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: balenthiran.co.uk
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    # The app exists to upload video. nginx-ingress defaults to a 1 MB request body and 60s
    # proxy timeouts, which rejects or cuts off essentially every real recording, so both are
    # raised. The backend has its own matching cap — Uploads__MaxBytes below.
    nginx.ingress.kubernetes.io/proxy-body-size: "2048m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    # snip-it is an open upload-and-transcode endpoint on a cluster shared with everything else,
    # so anyone who found the URL could spend Groq credits and fill the PVC. James's call (#13,
    # #14): basic auth in front of it, checked by nginx before a request reaches a pod. These
    # annotations apply to this Ingress object only — the paths below — so balenthiran.co.uk on
    # the same host is untouched. The secret is cluster-side and cannot live in this repo; see
    # docs/specs/deployment.md. If it is absent nginx answers 503 here, which is the right way
    # round for an app whose failure mode is a stranger's upload.
    nginx.ingress.kubernetes.io/auth-type: "basic"
    nginx.ingress.kubernetes.io/auth-secret: "snipit-basic-auth"
    nginx.ingress.kubernetes.io/auth-realm: "snip-it"
  tls:
    - hosts:
        - balenthiran.co.uk
      secretName: balenthiran-tls   # shared wildcard cert — no per-project TLS setup

# Uploaded sources, extracted audio and exported cuts are written to disk by
# LocalDiskFileStorageService. With no volume they live in the container's writable layer and go
# away on every restart — the job rows in Postgres survive, the media they point at doesn't.
persistence:
  enabled: true
  size: 20Gi
  storageClass: ""              # "" = cluster default StorageClass

apps:
  - name: backend
    image:
      # Must match docker-build-push.yml's BACKEND_IMAGE (snip-it-backend), which is not the
      # release name — the previous value (snipit-backend) does not exist in OCIR.
      repository: lhr.ocir.io/lr7uc6l49odc/snip-it-backend
      tag: latest
      pullPolicy: Always
    containerPort: 8080
    replicas: 1                 # single writer: the storage PVC is RWO and the job queue is in-process
    service:
      port: 80
      targetPort: 8080
    ingress:
      path: /snipit/api         # longer prefix than the frontend's, so ingress-nginx matches it first
      pathType: Prefix
    env:
      # The ingress forwards the whole path; UsePathBase strips this prefix before routing.
      - name: PathBase
        value: "/snipit"
      - name: FileStorage__RootPath
        value: "/data/storage"
      - name: Uploads__MaxBytes
        value: "2147483648"     # 2 GiB — same ceiling as proxy-body-size above
      - name: ConnectionStrings__DefaultConnection
        valueFrom:
          secretKeyRef:
            name: snipit-secrets
            key: DATABASE_URL
      - name: Groq__ApiKey
        valueFrom:
          secretKeyRef:
            name: snipit-secrets
            key: GROQ_API_KEY
    volumeMounts:
      - name: storage
        mountPath: /data/storage
    volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: snipit-storage
    resources: {}
    nodeSelector: {}
    tolerations: []
    affinity: {}

  - name: frontend
    image:
      # Must match docker-build-push.yml's FRONTEND_IMAGE (snip-it-frontend).
      repository: lhr.ocir.io/lr7uc6l49odc/snip-it-frontend
      tag: latest
      pullPolicy: Always
    containerPort: 80
    replicas: 1
    service:
      port: 80
      targetPort: 80
    ingress:
      path: /snipit             # nginx.conf serves the SPA from /usr/share/nginx/html/snipit
      pathType: Prefix
    resources: {}
    nodeSelector: {}
    tolerations: []
    affinity: {}
